export default function ScenePage() {
  return (
    <section className="page-shell">
      <span className="eyebrow bullet">The Scene · Chicago</span>
      <h1 className="display">
        The map is being printed.
      </h1>
      <p className="lede">
        Chicago events on a Mapbox map ship in Phase 4. This page currently
        exists to verify the shared chrome (and that anon visitors can reach
        it without auth).
      </p>
    </section>
  );
}
