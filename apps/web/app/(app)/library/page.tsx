export default function LibraryPage() {
  return (
    <section className="page-shell">
      <span className="eyebrow bullet">The Library · Coming next</span>
      <h1 className="display">
        Search is on its way.
      </h1>
      <p className="lede">
        YouTube search, bookmarks, and playlists ship in Phase 3. This page
        currently exists to verify the shared chrome and the (app) layout’s
        ensureUser() call.
      </p>
    </section>
  );
}
